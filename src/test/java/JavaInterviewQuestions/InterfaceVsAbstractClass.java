package JavaInterviewQuestions;

public class InterfaceVsAbstractClass {

	
	// If we don't know anything about implementation we should go for interface and we just have requirement specification 
	// If we know partial implementation we should go for Abstract class 
	
	
	// if your class is not fully implemented then we can not create the object of that class 
	// interface does not have single method implemented that is why we can not create object of it
	
}
