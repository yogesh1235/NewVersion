package JavaInterviewQuestions;

public class DiffBetwenfinalFinallyFinalize {
	
	
	// Final
	// If a variable is declared as final we can not do reassignment 
	//if a method is declared as final we can not override in the child class
	//if a class is declared as final we can not extend the class 
	
	
	//Finally 
	// finally is a block associated with try catch 
	// we can write risky code under the finally block which may raise exception 
	
	// finalize 
	//garbage collector call this method for cleanup activity before destroying any object 

}
